<li><%= link_to(image_tag(incident_block.image.image.url, :size => "155x125", :class => "incimg"),incident_report_path(incident_block)) %> <%= incident_block.description %> <br /><small>Has <%= incident_block.comments.count %> comments and <%= incident_block.dislikers.count %> people dislike it too.</small></li> <!-- we should have a nice badge for the dislike counter (like the retweet count badge) -->
