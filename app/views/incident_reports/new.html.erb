<%= image_tag("banner.png", :alt => configatron.app_name) %>


<%= form_for(@incident_report, :html => { :multipart => true }) do |form| %>
	<%= form.text_field :image, :as => :file %>
  <%= form.submit %>
<% end %>
