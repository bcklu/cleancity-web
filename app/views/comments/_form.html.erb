<%= semantic_form_for(comment, :url => incident_report_comments_path(comment.incident_report), :remote => true) do |f| %>
	<%= f.inputs do %>
		<%= f.input :body %>
  <% end %>
	<%= f.buttons %>
<% end %>